{
  "name": "bitify-api",
  "private": true,
  "scripts": {
    "dev": "set ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000 && .venv\\Scripts\\python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --reload-dir app",
    "dev:noreload": "set ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000 && .venv\\Scripts\\python -m uvicorn app.main:app --host 0.0.0.0 --port 8000"
  }
}